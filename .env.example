# Copie este arquivo para ".env" e preencha com sua chave.
# Sem isso, os comandos /info, /top e o download automático de capas não funcionam
# (a busca e o download de ROMs continuam funcionando normalmente).

# Chave gratuita em https://rawg.io/apidocs (cadastro simples, sem cartão)
RAWG_API_KEY=
